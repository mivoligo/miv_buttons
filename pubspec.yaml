name: miv_buttons
description: "3D-looking buttons"
version: 0.1.0
homepage: https://github.com/mivoligo/miv_buttons

environment:
  sdk: ^3.5.1
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
